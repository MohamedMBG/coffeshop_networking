<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="20dp" />   <!-- Change radius here -->
    <solid android:color="@android:color/white" /> <!-- Background behind image -->
</shape>
