<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="2dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="16dp"
        android:gravity="center_vertical">

        <!-- Leading Icon Container -->
        <FrameLayout
            android:layout_width="48dp"
            android:layout_height="48dp">

            <!-- Background Circle (changes color based on activity type) -->
            <View
                android:id="@+id/iconBackground"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@drawable/circle_background_earn"
                tools:background="@drawable/circle_background_earn"/>

            <!-- Activity Type Icon -->
            <ImageView
                android:id="@+id/activityIcon"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_scan"
                android:contentDescription="Activity type"
                app:tint="@android:color/white"
                tools:src="@drawable/ic_scan"/>
        </FrameLayout>

        <!-- Content Section -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:layout_marginStart="16dp">

            <!-- Activity Title -->
            <TextView
                android:id="@+id/activityTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Scan — Simple Coffee"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="#212121"
                android:maxLines="1"
                android:ellipsize="end"
                tools:text="Scan — Simple Coffee"/>

            <!-- DateTime Subtitle -->
            <TextView
                android:id="@+id/activityDateTime"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Sat, 01 Nov • 10:32"
                android:textSize="13sp"
                android:textColor="#757575"
                android:layout_marginTop="4dp"
                tools:text="Sat, 01 Nov • 10:32"/>

            <!-- Optional: Additional details (location, store, etc.) -->
            <TextView
                android:id="@+id/activityDetails"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Downtown Location"
                android:textSize="12sp"
                android:textColor="#9E9E9E"
                android:layout_marginTop="2dp"
                android:visibility="gone"
                tools:text="Downtown Location"
                tools:visibility="visible"/>
        </LinearLayout>

        <!-- Points Amount -->
        <TextView
            android:id="@+id/activityPoints"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="+5"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="#4CAF50"
            android:layout_marginStart="12dp"
            tools:text="+5"/>
    </LinearLayout>
</androidx.cardview.widget.CardView>